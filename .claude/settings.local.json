{
  "permissions": {
    "allow": [
      "Bash(\"C:/xampp/mysql/bin/mysql.exe\" -u root hospital_db -e \"ALTER TABLE users ADD COLUMN IF NOT EXISTS password_reset_token VARCHAR\\(64\\) DEFAULT NULL; ALTER TABLE users ADD COLUMN IF NOT EXISTS password_reset_expires DATETIME DEFAULT NULL;\")"
    ]
  }
}
